extends ../layout

block content
  h1.p-4 Registration Form

  form(method="POST").p-2
    .form-group.ml-3
      label(for="usernameInput") Email
      input.form-control#usernameInput(
        name="username"
        type="email"
        placeholder="Enter email"
        class=errors.username ? "is-invalid" : ""
        value=user.username
      )
      if errors.username
        #usernameInput.invalid-feedback= errors.username.message

    .form-group.ml-3
      label(for="password") Password
      input.form-control#password(
        name="password"
        type="password"
        placeholder="Password"
        class=errors.password ? "is-invalid" : ""
      )
      if errors.password
        #password.invalid-feedback= errors.password.message

    .form-group.ml-3
      label(for="confirmPassword") Password
      input.form-control#confirmPassword(
        name="confirmPassword"
        type="password"
        placeholder="Confirm Password"
      )

    button.btn.btn-primary.ml-3(type="submit") Submit
    a.button.px-5(href="/login" type="button") Already Have An Account